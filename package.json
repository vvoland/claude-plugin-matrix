{
  "name": "claude-channel-matrix",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "npm install --no-audit --no-fund --silent && bun server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "matrix-bot-sdk": "^0.8.0",
    "@matrix-org/matrix-sdk-crypto-nodejs": "^0.4.0"
  }
}
